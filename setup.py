from setuptools import setup, find_packages

setup(
    name='python-send-email',
    author='Rikuto',
    url='https://github.com/rikuto125/python-send-email.git',
    version='0.1',
    packages=find_packages(),
    install_requires=[
    ],
)
